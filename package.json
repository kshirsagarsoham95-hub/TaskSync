{
  "name": "tasksync",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "node --experimental-sqlite server/server.js",
    "dev": "nodemon --exec \"node --experimental-sqlite\" server/server.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
